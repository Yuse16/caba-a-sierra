import { PanelPromotionsLayout } from "@/components/panel/panel-promotions-layout"

export default function PromotionsLayout({ children }: { children: React.ReactNode }) {
  return <PanelPromotionsLayout>{children}</PanelPromotionsLayout>
}
