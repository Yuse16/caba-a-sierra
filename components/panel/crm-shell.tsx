"use client"
import Link from "next/link"
import { usePathname } from "next/navigation"
import { FileText, Home, Mountain, Settings, Users } from "lucide-react"
import { PanelLogoutButton } from "@/components/auth/panel-logout-button"
import { cn } from "@/lib/utils"
export function CrmShell({ children }: { children: React.ReactNode }) { const pathname = usePathname(); return <div className="min-h-screen bg-background text-foreground"><header className="sticky top-0 z-50 border-b border-border bg-background/95 pt-[env(safe-area-inset-top)] backdrop-blur"><div className="mx-auto flex min-h-16 max-w-7xl items-center gap-2 px-4 sm:px-6"><span className="inline-flex size-10 items-center justify-center rounded-xl bg-primary/10 text-primary"><Mountain className="size-5" aria-hidden /></span><span className="hidden text-sm font-semibold min-[360px]:inline">DUPEZ</span><nav className="ml-auto flex gap-1" aria-label="CRM"><Link href="/panel" aria-label="Inicio" className="inline-flex size-11 items-center justify-center rounded-lg hover:bg-muted"><Home className="size-4" /></Link><Link href="/panel/solicitudes" aria-label="Solicitudes" className={cn("inline-flex min-h-11 items-center gap-2 rounded-lg px-3 text-sm font-medium hover:bg-muted",pathname.startsWith("/panel/solicitudes")&&"bg-primary text-primary-foreground")}><FileText className="size-4"/><span className="hidden sm:inline">Solicitudes</span></Link><Link href="/panel/clientes" aria-label="Clientes" className={cn("inline-flex min-h-11 items-center gap-2 rounded-lg px-3 text-sm font-medium hover:bg-muted",pathname.startsWith("/panel/clientes")&&"bg-primary text-primary-foreground")}><Users className="size-4"/><span className="hidden sm:inline">Clientes</span></Link><Link href="/panel/configuracion" aria-label="Configuración" className={cn("inline-flex min-h-11 items-center gap-2 rounded-lg px-3 text-sm font-medium hover:bg-muted",pathname.startsWith("/panel/configuracion")&&"bg-primary text-primary-foreground")}><Settings className="size-4"/><span className="hidden sm:inline">Configuración</span></Link></nav><PanelLogoutButton /></div></header>{children}</div> }
